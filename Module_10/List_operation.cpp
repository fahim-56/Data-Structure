#include <bits/stdc++.h>
using namespace std;
int main()
{
    list<int> Mylist = {10, 30, 50, 40, 20, 20, 30};
    // Mylist.remove(20);
    // Mylist.sort();
    // Mylist.sort(greater<int>());
    // Mylist.unique();

    // Mylist.sort(greater<int>());
    // Mylist.unique();

    // Mylist.reverse();

    for (int x : Mylist)
        cout << x << " ";
    return 0;
}